{
  "name": "code-review-report",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Cursor plugin that writes code review reports for vulnerabilities and optimizations.",
  "scripts": {
    "test": "node --test scripts/review-tools.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "Apache-2.0"
}
